import RentProperties from ".";

export const metadata = {
  title: "Premium Realtor | Explore International Properties",
};

const Page = () => {
  return <RentProperties />;
};

export default Page;
