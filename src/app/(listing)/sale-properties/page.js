import SaleProperties from ".";

export const metadata = {
  title: "Premium Realtor | Explore International Properties",
};

const Page = () => {
  return <SaleProperties />;
};

export default Page;
